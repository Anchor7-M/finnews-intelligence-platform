{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FinNews market signal row v1",
  "type": "object",
  "required": [
    "signal_id",
    "impact_id",
    "event_id",
    "asset_id",
    "direction",
    "horizon",
    "status",
    "information_cutoff_at",
    "generated_at",
    "provider",
    "provider_version",
    "payload_hash",
    "idempotency_key",
    "synthetic"
  ],
  "properties": {
    "id": { "type": "string" },
    "signal_id": { "type": "string" },
    "impact_id": { "type": "string" },
    "event_id": { "type": "string" },
    "asset_id": { "type": "string" },
    "direction": { "enum": ["positive", "negative", "mixed", "uncertain"] },
    "horizon": { "enum": ["intraday", "one_day", "one_week", "one_month"] },
    "status": { "enum": ["informational", "research", "abstained", "rejected", "expired"] },
    "confidence": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
    "score": { "type": ["number", "null"], "minimum": 0, "maximum": 1 },
    "information_cutoff_at": { "type": "string" },
    "generated_at": { "type": "string" },
    "expires_at": { "type": "string" },
    "provider": { "type": "string" },
    "provider_version": { "type": "string" },
    "evidence_codes": { "type": "array", "items": { "type": "string" } },
    "quality_tags": { "type": "array", "items": { "type": "string" } },
    "risk_tags": { "type": "array", "items": { "type": "string" } },
    "payload_hash": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "idempotency_key": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "synthetic": { "const": true }
  },
  "not": {
    "anyOf": [
      { "required": ["order"] },
      { "required": ["order_type"] },
      { "required": ["position_size"] },
      { "required": ["volume"] },
      { "required": ["stop_loss"] },
      { "required": ["take_profit"] },
      { "required": ["account"] },
      { "required": ["password"] }
    ]
  },
  "additionalProperties": false
}
